name: Dev Build

on:
  push:
    branches: [ dev ]
    paths: [ src/** ]

jobs:
  deploy:
    uses: magemonkeystudio/.github/.github/workflows/reusable-devbuild.yml@main
    secrets: inherit
    with:
      update_javadocs: true
